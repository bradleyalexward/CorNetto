% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-data.R
\name{filterSamples}
\alias{filterSamples}
\title{Filter Samples Before Network Construction}
\usage{
filterSamples(
  analysisData,
  sampleIds = NULL,
  groupColumn = NULL,
  groupLevels = NULL,
  dropUnusedLevels = TRUE
)
}
\arguments{
\item{analysisData}{A \code{MultiAssayExperiment}.}

\item{sampleIds}{Optional sample identifiers to retain.}

\item{groupColumn}{Optional sample metadata column used to retain
samples by group membership.}

\item{groupLevels}{Optional group labels to retain from \code{groupColumn}.
When \code{NULL} and \code{groupColumn} is supplied, samples with non-missing
and non-empty values in \code{groupColumn} are retained.}

\item{dropUnusedLevels}{Whether to drop unused factor levels in
sample metadata after filtering.}
}
\value{
A filtered \code{MultiAssayExperiment}.
}
\description{
Filter a CorNetto analysis object to selected samples while allowing
assays to retain different overlapping subsets of those samples.
}
\examples{
analysisData <- exampleAnalysisData()
filtered <- filterSamples(
    analysisData,
    groupColumn = "clinicalGroup",
    groupLevels = "PASC"
)
summarizeAnalysisData(
    filtered,
    groupColumn = "clinicalGroup",
    quiet = TRUE
)$samples
}
