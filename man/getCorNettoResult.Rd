% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-internal.R
\name{getCorNettoResult}
\alias{getCorNettoResult}
\alias{knowledgeNetworks}
\alias{correlationResults}
\alias{differentialCorrelationResults}
\alias{differentialCorrelationNetworks}
\alias{rewiringResults}
\alias{validationResults}
\alias{integratedNetworks}
\title{Get Stored CorNetto Results}
\usage{
getCorNettoResult(analysisData, resultType = NULL, resultName = NULL)

knowledgeNetworks(analysisData)

correlationResults(analysisData)

differentialCorrelationResults(analysisData)

differentialCorrelationNetworks(analysisData)

rewiringResults(analysisData)

validationResults(analysisData)

integratedNetworks(analysisData)
}
\arguments{
\item{analysisData}{A \code{MultiAssayExperiment} object.}

\item{resultType}{Optional result type. When \code{NULL}, the complete
result store is returned unless \code{resultName} is supplied. Common
values include \code{"knowledgeNetworks"}, \code{"correlationResults"},
\code{"differentialCorrelationResults"},
\code{"differentialCorrelationNetworks"}, \code{"rewiringResults"},
\code{"validationResults"}, and \code{"integratedNetworks"}.}

\item{resultName}{Optional stored result name. When supplied with
\code{resultType}, a single result is returned. When supplied without
\code{resultType}, all result slots are searched and the result name must
be unique.}
}
\value{
A named list of stored CorNetto results, a single result-type
list, or one stored result object.
}
\description{
Retrieve the complete CorNetto result store, a result-type slot, or a
single named stored result from a \code{MultiAssayExperiment}.
}
\examples{
analysisData <- exampleAnalysisData()
analysisData <- validateKnowledgeNetwork(
    exampleKnowledgeNetwork(),
    analysisData = analysisData,
    storeResult = TRUE
)
names(getCorNettoResult(analysisData))
names(knowledgeNetworks(analysisData))
getCorNettoResult(analysisData, "knowledgeNetworks", "knowledgeNetwork")
}
