% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-integration.R
\name{combineNetworks}
\alias{combineNetworks}
\title{Combine Network Edge Tables}
\usage{
combineNetworks(
  analysisData = NULL,
  knowledgeNetwork = NULL,
  correlationNetwork = NULL,
  differentialCorrelationNetwork = NULL,
  networkList = NULL,
  includeReverseEdges = TRUE,
  removeDuplicates = TRUE,
  resultName = "combinedNetwork",
  storeResult = FALSE
)
}
\arguments{
\item{analysisData}{Optional \code{MultiAssayExperiment} used to retrieve
stored CorNetto results and optionally store the combined network.}

\item{knowledgeNetwork}{Optional knowledge-network edge table or list
of edge tables.}

\item{correlationNetwork}{Optional correlation edge table or list of
edge tables.}

\item{differentialCorrelationNetwork}{Optional differential
correlation edge table or list of edge tables.}

\item{networkList}{Optional additional edge table or list of edge
tables.}

\item{includeReverseEdges}{Whether to duplicate undirected edges to
support directed graph traversal. See Details before enabling it for a
table you intend to score.}

\item{removeDuplicates}{Whether to remove exactly duplicated edge
rows after standardization.}

\item{resultName}{Name used when storing the combined network.}

\item{storeResult}{Whether to store the result in \code{analysisData}.}
}
\value{
A standardized edge \code{DataFrame} or an updated
\code{MultiAssayExperiment}.
}
\description{
Combine knowledge edges, correlation edges, differential-correlation
edges, or other standardized CorNetto edge tables into one network.
}
\details{
\code{includeReverseEdges = TRUE} appends a reversed copy of every
undirected edge. That is what you want for traversal and for export to
tools that expect an explicit adjacency list, and it is the default for
that reason.

It is not what you want for anything that treats the table as a set of
edges. \code{\link[=calculateRewiringScores]{calculateRewiringScores()}} rejects duplicate or mirrored edges
because they would double every node's degree and multiply
\code{rawRewiringScore} by \code{sqrt(2)}. An undirected \code{\link[=createNetworkGraph]{createNetworkGraph()}} built
from a mirrored table gains parallel edges.
Score first, then combine with reverse edges for export, or pass
\code{includeReverseEdges = FALSE} and let \code{\link[=createNetworkGraph]{createNetworkGraph()}} handle
direction.
}
\examples{
analysisData <- exampleAnalysisData()
knowledgeNetwork <- exampleKnowledgeNetwork()
differentialResults <- testDifferentialCorrelation(
    analysisData = analysisData,
    groupColumn = "clinicalGroup",
    groupLevels = c("Recovered", "PASC"),
    assayName = "protein",
    minimumAbsoluteCorrelation = 0,
    adjustedPValueThreshold = 1,
    pAdjustMethod = "fdr",
    storeResult = FALSE
)
differentialNetwork <- createDifferentialCorrelationNetwork(
    differentialResults,
    minimumAbsoluteCorrelation = 0
)
combinedNetwork <- combineNetworks(
    knowledgeNetwork = knowledgeNetwork,
    differentialCorrelationNetwork = differentialNetwork
)
combinedNetwork
}
